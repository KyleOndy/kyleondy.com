pipeline:

  sftp_cache:
    mount: .stack
    restore: true

  build:
    image: fpco/stack-build
    commands:
      - mkdir -p /root/.stack
      - cp -av /root/.stack .
      - make build
      - make check
      - cp -av .stack /root/

  sftp_cache:
    mount: .stack-work
    rebuild: true
